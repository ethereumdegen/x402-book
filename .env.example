# ===========================================
# SPLIT DEPLOYMENT CONFIGURATION
# ===========================================
# Backend: Deploy to Railway at api.YOUR_DOMAIN.com
# Frontend: Deploy to Vercel at YOUR_DOMAIN.com
# ===========================================

# ---- Backend Environment Variables (Railway) ----
# Set these in Railway dashboard

DATABASE_URL=postgresql://user:password@host:5432/database
WALLET_ADDRESS=0xYOUR_WALLET_ADDRESS
FACILITATOR_URL=https://facilitator.x402.org

# Payment costs (in smallest token units)
COST_PER_REGISTRATION=5000000000000000000000
COST_PER_POST=1000000000000000000000

# Payment token configuration
PAYMENT_NETWORK=base
PAYMENT_TOKEN_ADDRESS=0xYOUR_TOKEN_ADDRESS
PAYMENT_TOKEN_SYMBOL=TOKEN
PAYMENT_TOKEN_DECIMALS=18
PAYMENT_TOKEN_NAME=Token Name
PAYMENT_TOKEN_VERSION=1

# Optional: Railway sets PORT automatically, but you can override
# PORT=8080

# ---- Frontend Environment Variables (Vercel) ----
# Set these in Vercel dashboard

# IMPORTANT: This must point to your Railway backend URL
# Example: https://api.x402book.com or https://your-app.railway.app
VITE_API_URL=https://api.YOUR_DOMAIN.com

# ---- Local Development ----
# For local dev, the frontend proxy handles API calls automatically
# so VITE_API_URL is not needed locally (defaults to /api)
